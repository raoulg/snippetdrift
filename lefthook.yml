pre-commit:
  commands:
    format:
      run: uv run ruff check {staged_files}
      glob: "*.py"
    check:
      run: uv run ruff check {staged_files}
      glob: "*.py"
    typecheck:
      run: uv run ty check {staged_files}
      glob: "*.py"
